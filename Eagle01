<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>แบบทดสอบคณิตศาสตร์</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 20px;
            background-color: #f4f4f4;
            color: #333;
        }
        .container {
            max-width: 800px;
            margin: auto;
            background: #fff;
            padding: 20px 30px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .logo-container {
            text-align: center;
            margin-bottom: 20px;
        }
        .logo-container img {
            max-width: 120px;
            height: auto;
        }
        h1, h2 {
            text-align: center;
            color: #4CAF50;
        }
        .user-info {
            margin-bottom: 20px;
            text-align: center;
        }
        .user-info input[type="text"] {
            padding: 8px;
            margin-top: 5px;
            width: 80%;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .question {
            margin-bottom: 20px;
            padding: 15px;
            background-color: #eef5ee;
            border-left: 5px solid #4CAF50;
            border-radius: 5px;
        }
        .question-text {
            font-size: 1.1em;
            margin-bottom: 10px;
        }
        .options label {
            display: block;
            margin: 5px 0;
            padding: 10px;
            background-color: #f9f9f9;
            border: 1px solid #ddd;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .options label:hover {
            background-color: #f0f0f0;
        }
        .options input[type="radio"] {
            margin-right: 10px;
        }
        .result-message {
            margin-top: 15px;
            padding: 10px;
            border-radius: 4px;
            font-weight: bold;
        }
        .correct {
            color: #28a745;
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
        }
        .incorrect {
            color: #dc3545;
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
        }
        button {
            display: block;
            width: 100%;
            padding: 12px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1.2em;
            margin-top: 20px;
        }
        button:hover {
            background-color: #45a049;
        }
        button:disabled {
            background-color: #cccccc;
            cursor: not-allowed;
        }
        .summary {
            margin-top: 30px;
            padding: 20px;
            background-color: #e9e9e9;
            border-radius: 8px;
            text-align: center;
        }
    </style>
    <script type="text/javascript" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
    <script>
        MathJax = {
            tex: {
                inlineMath: [['$', '$'], ['\\(', '\\)']]
            }
        };

        const questions = [
            {
                "id": "q1",
                "text": "กำหนดให้ \\(f(x) = \\begin{cases} g(x) + \\displaystyle\\frac{\\sqrt{x+5}-3}{x-4}; & x > 4 \\\\[2mm] \\displaystyle\\frac{x^{3}-64}{x-4}; & 0 < x < 4 \\end{cases}\\) <br>ถ้า \\(f\\) มีลิมิตที่ \\(x=4\\) แล้ว \\(\\displaystyle \\lim_{x\\to 4^{+}} g(x) \\cdot \\lim_{x\\to 4^{+}} x \\) มีค่าเท่าใด",
                "options": [
                    { "text": "$\\displaystyle \\frac{287}{6}$", "isCorrect": false },
                    { "text": "$\\displaystyle 48$", "isCorrect": false },
                    { "text": "$\\displaystyle \\frac{574}{3}$", "isCorrect": true },
                    { "text": "$\\displaystyle 192$", "isCorrect": false }
                ]
            },
            {
                "id": "q2",
                "text": "กำหนดให้ \\(a\\) และ \\(b\\) เป็นจำนวนจริง และ \\(f\\) เป็นฟังก์ชัน โดยที่ \\(f(x) = \\begin{cases} \\displaystyle \\frac{x^{2}-4}{|x-2|}; & -2< x <2 \\\\[2mm] ax+b; & 2\\leqslant x <5 \\\\[2mm] 15; & x\\geq 5 \\end{cases}\\) <br>ถ้า \\(f\\) เป็นฟังก์ชันต่อเนื่องบนช่วง \\((-2 ,\\infty)\\) แล้วค่าของ \\(a^{2}-b\\) มีค่าเท่าใด",
                "options": [
                    { "text": "$-\\frac{151}{9}$", "isCorrect": false },
                    { "text": "$\\frac{511}{9}$", "isCorrect": true },
                    { "text": "$\\frac{411}{9}$", "isCorrect": false },
                    { "text": "$-\\frac{50}{3}$", "isCorrect": false }
                ]
            },
            {
                "id": "q3",
                "text": "กำหนดให้ \\(f(x) = \\begin{cases} \\displaystyle \\frac{x-4}{\\sqrt{x+5}-\\sqrt{2x+1}}; & x\\neq 4 \\\\[2mm] a; & x=4 \\end{cases}\\) <br> โดยที่ \\(a\\) เป็นจำนวนจริง ถ้า \\(f\\) เป็นฟังก์ชันต่อเนื่องที่จุด \\(x=4\\) แล้ว \\(a\\) มีค่าเท่าใด",
                "options": [
                    { "text": "$6$", "isCorrect": false },
                    { "text": "$3$", "isCorrect": false },
                    { "text": "$-3$", "isCorrect": false },
                    { "text": "$-6$", "isCorrect": true }
                ]
            },
            {
                "id": "q4",
                "text": "กำหนดให้ \\(f(x) = \\begin{cases} g(x); & x\\leq -1 \\\\[2mm] \\displaystyle \\frac{x^{4}-3x^{3}-7x^{2}+15x+18}{x^{2}-x-2}; & -1< x <3 \\\\[2mm] h(x); & x\\geq 3 \\end{cases}\\) และ \\(p(x)=3x^{2}-2 \\) ถ้า \\(f\\) เป็นฟังก์ชันต่อเนื่องบนจำนวนจริง แล้วค่าของ \\(\\displaystyle \\lim_{x\\rightarrow -1^{-}}(p\\circ g)(x)+\\lim_{x\\rightarrow 3^{+}}(p\\circ h)(x)\\) มีค่าเท่าใด",
                "options": [
                    { "text": "$\\frac{244}{3}$", "isCorrect": true },
                    { "text": "$\\frac{250}{3}$", "isCorrect": false },
                    { "text": "$81$", "isCorrect": false },
                    { "text": "$\\frac{256}{3}$", "isCorrect": false }
                ]
            },
            {
                "id": "q5",
                "text": "กำหนดให้ \\(f(x) = \\begin{cases} \\displaystyle \\frac{\\sqrt{ax^{2}+6}-5}{x-2}; & x <2 \\\\[2mm] b; & x=2 \\\\[2mm] \\displaystyle \\frac{c}{x^{2}-1}; & x >2 \\end{cases}\\) และ \\(a,b,c\\) เป็นจำนวนจริงที่ทำให้ \\(f\\) เป็นฟังก์ชันต่อเนื่องที่ \\(x=2\\) จงหาค่า \\(\\displaystyle \\frac{a}{b}-c\\)",
                "options": [
                    { "text": "$\\frac{16}{5}$", "isCorrect": false },
                    { "text": "$-\\frac{32}{10}$", "isCorrect": false },
                    { "text": "$-\\frac{16}{5}$", "isCorrect": true },
                    { "text": "$\\frac{25}{10}$", "isCorrect": false }
                ]
            },
            {
                "id": "q6",
                "text": "ถ้า \\(f(x)=\\sqrt[3]{x}+3\\sqrt{x}-2\\) แล้วค่าของ \\(f'(64)\\) มีค่าเท่าใด",
                "options": [
                    { "text": "$\\frac{5}{24}$", "isCorrect": true },
                    { "text": "$\\frac{10}{48}$", "isCorrect": false },
                    { "text": "$\\frac{7}{24}$", "isCorrect": false },
                    { "text": "$\\frac{11}{48}$", "isCorrect": false }
                ]
            },
            {
                "id": "q7",
                "text": "ถ้า \\(f(x)=(2\\sqrt{x}+x)(x-2)\\) แล้วค่าของ \\(f'(4)\\) มีค่าเท่าใด",
                "options": [
                    { "text": "$8$", "isCorrect": false },
                    { "text": "$11$", "isCorrect": true },
                    { "text": "$13$", "isCorrect": false },
                    { "text": "$15$", "isCorrect": false }
                ]
            },
            {
                "id": "q8",
                "text": "ถ้า \\(f(x)=(2-x^{2})^{2}(x^{3}+x-1)+4x-3\\) แล้วค่าของ \\(f'(0)\\) มีค่าเท่าใด",
                "options": [
                    { "text": "$4$", "isCorrect": false },
                    { "text": "$6$", "isCorrect": false },
                    { "text": "$8$", "isCorrect": true },
                    { "text": "$10$", "isCorrect": false }
                ]
            },
            {
                "id": "q9",
                "text": "ถ้า \\(f(x)=\\displaystyle \\frac{\\sqrt{x}-3}{\\sqrt{x}+1}\\) แล้วค่าของ \\(f'(9)\\) มีค่าเท่าใด",
                "options": [
                    { "text": "$\\frac{1}{12}$", "isCorrect": false },
                    { "text": "$\\frac{1}{24}$", "isCorrect": true },
                    { "text": "$\\frac{2}{48}$", "isCorrect": false },
                    { "text": "$\\frac{1}{16}$", "isCorrect": false }
                ]
            },
            {
                "id": "q10",
                "text": "ถ้า \\(f(x)=\\displaystyle \\frac{x^{2}-5x}{(x^{2}+2)(x+3)}\\) แล้วค่าของ \\(f'(0)\\) มีค่าเท่าใด",
                "options": [
                    { "text": "$-\\frac{5}{6}$", "isCorrect": true },
                    { "text": "$\\frac{5}{6}$", "isCorrect": false },
                    { "text": "$-\\frac{30}{36}$", "isCorrect": false },
                    { "text": "$-\\frac{25}{36}$", "isCorrect": false }
                ]
            }
        ];

        document.addEventListener('DOMContentLoaded', () => {
            const quizContainer = document.getElementById('quiz-container');
            const summaryContainer = document.getElementById('quiz-summary');
            
            const userInfoDiv = document.createElement('div');
            userInfoDiv.className = 'user-info';
            userInfoDiv.innerHTML = `
                <h3>แบบฝึกทักษะสำหรับซ่อมสอบเก็บคะแนน</h3>
                <label for="name">ชื่อ-นามสกุล:</label>
                <input type="text" id="name" name="name" placeholder="กรอกชื่อ-นามสกุล">
            `;
            quizContainer.before(userInfoDiv);

            questions.forEach((q, index) => {
                const questionDiv = document.createElement('div');
                questionDiv.className = 'question';
                questionDiv.id = q.id;
                
                const questionTextDiv = document.createElement('div');
                questionTextDiv.className = 'question-text';
                questionTextDiv.innerHTML = `<b>ข้อที่ ${q.id.substring(1)}.</b> ${q.text}`;
                questionDiv.appendChild(questionTextDiv);

                const optionsDiv = document.createElement('div');
                optionsDiv.className = 'options';

                if (q.options) {
                    q.options.forEach((option, optionIndex) => {
                        const label = document.createElement('label');
                        label.innerHTML = `<input type="radio" name="${q.id}" value="${optionIndex}"> ${option.text}`;
                        optionsDiv.appendChild(label);
                    });
                }
                questionDiv.appendChild(optionsDiv);
                quizContainer.appendChild(questionDiv);
            });

            document.getElementById('submit-button').addEventListener('click', function() {
                const userName = document.getElementById('name').value.trim();
                if (!userName) {
                    alert('กรุณากรอกชื่อ-นามสกุล');
                    return;
                }

                let score = 0;
                const totalQuestions = questions.length;

                questions.forEach(q => {
                    const questionDiv = document.getElementById(q.id);
                    const oldResult = questionDiv.querySelector('.result-message');
                    if (oldResult) {
                        oldResult.remove();
                    }

                    const resultDiv = document.createElement('div');
                    resultDiv.className = 'result-message';
                    
                    if (q.options) {
                        const radioButtons = document.querySelectorAll(`input[name="${q.id}"]`);
                        const selectedOption = document.querySelector(`input[name="${q.id}"]:checked`);

                        if (selectedOption) {
                            const selectedIndex = parseInt(selectedOption.value);
                            if (q.options[selectedIndex].isCorrect) {
                                score++;
                                resultDiv.classList.add('correct');
                                resultDiv.textContent = '✔ ถูกต้อง';
                            } else {
                                resultDiv.classList.add('incorrect');
                                resultDiv.innerHTML = `❌ ไม่ถูกต้อง`;
                            }
                        } else {
                            resultDiv.classList.add('incorrect');
                            resultDiv.textContent = 'กรุณาเลือกคำตอบ';
                        }
                        radioButtons.forEach(rb => rb.disabled = true);
                        questionDiv.appendChild(resultDiv);
                    }
                });

                summaryContainer.innerHTML = `<h2>ผลการทดสอบของคุณ ${userName}</h2>
                                              <h2>คะแนนที่ได้: ${score} / ${totalQuestions}</h2>`;
                
                this.disabled = true;
                this.textContent = 'ส่งคำตอบแล้ว';
                
                MathJax.typesetPromise();
            });
        });
    </script>
</head>
<body>
    <div class="container">
        <div class="logo-container">
            <img src="https://i.postimg.cc/Y9Fs6wJb/2.png" alt="Department of Mathematics Logo">
        </div>
        <h1> แบบฝึกทักษะสำหรับซ่อมสอบเก็บคะแนน</h1>
        <div id="quiz-container"></div>
        <button id="submit-button">ส่งคำตอบ</button>
        <div id="quiz-summary"></div>
    </div>
</body>
</html>
